<%= simple_form_for(@private_message) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :to, collection: @userEmails %>
    <%= f.input :content %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
